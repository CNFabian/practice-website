/**
 * Design reference dimensions for fixed-layout Phaser scenes.
 *
 * Components are positioned using these reference dimensions so that
 * the layout never "squishes" on smaller viewports. When the actual
 * viewport is smaller than these values the canvas keeps the reference
 * size and the parent container scrolls instead.
 */
export const DESIGN_WIDTH = 1280;
export const DESIGN_HEIGHT = 720;
